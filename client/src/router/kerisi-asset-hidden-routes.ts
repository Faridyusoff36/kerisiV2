import type { RouteRecordRaw } from "vue-router";

/**
 * HIDDEN_PAGE_LEVEL4 Asset routes were inlined into `router/index.ts` (before
 * `...kerisiAssetHiddenRoutes`) so they sit next to LEVEL3 Asset shells and use
 * `AssetLegacyPlaceholderView` with the same props pattern.
 */
export const kerisiAssetHiddenRoutes: RouteRecordRaw[] = [];
