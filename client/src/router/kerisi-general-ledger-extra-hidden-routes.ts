import type { RouteRecordRaw } from "vue-router";

/**
 * HIDDEN_PAGE_LEVEL4 General Ledger (financial statement stubs) routes were inlined into
 * `router/index.ts` so they use `GeneralLedgerLegacyPlaceholderView`, not GenericLegacyPlaceholderView.
 */
export const kerisiGeneralLedgerExtraHiddenRoutes: RouteRecordRaw[] = [];
