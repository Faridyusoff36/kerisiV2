import type { RouteRecordRaw } from "vue-router";

/**
 * HIDDEN_PAGE_LEVEL4 Credit Control routes were inlined into `router/index.ts` (before
 * `...kerisiCreditControlHiddenRoutes`) so they sit next to LEVEL3 CC shells and use
 * `CreditControlLegacyPlaceholderView` with the same props pattern.
 */
export const kerisiCreditControlHiddenRoutes: RouteRecordRaw[] = [];
