import type { RouteRecordRaw } from "vue-router";

/**
 * HIDDEN_PAGE_LEVEL2–5: `Menu` prefix `Setup and Maintenance>`.
 *
 * All former Level-3 placeholder entries are registered as real routes in
 * `router/index.ts` (before this spread) so they override any legacy stub.
 */
export const kerisiSetupMaintenanceHiddenRoutes: RouteRecordRaw[] = [];
