import type { RouteRecordRaw } from "vue-router";

/**
 * `HIDDEN_PAGE_LEVEL2–5` Project Monitoring rows were generated here; **Level 3**
 * and **Level 4** entries are registered explicitly in `router/index.ts` before this spread.
 */
export const kerisiProjectMonitoringHiddenRoutes: RouteRecordRaw[] = [];
