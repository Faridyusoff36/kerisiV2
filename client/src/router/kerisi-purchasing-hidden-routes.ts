import type { RouteRecordRaw } from "vue-router";

/**
 * `HIDDEN_PAGE_LEVEL2–5` Purchasing rows were generated here; **Level 3**
 * entries are registered explicitly in `router/index.ts` before this spread.
 */
export const kerisiPurchasingHiddenRoutes: RouteRecordRaw[] = [];
