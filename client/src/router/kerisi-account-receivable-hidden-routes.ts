import type { RouteRecordRaw } from "vue-router";

/**
 * HIDDEN_PAGE_LEVEL4 Account Receivable routes were inlined into `router/index.ts` so they use
 * `AccountReceivableLegacyPlaceholderView`, not GenericLegacyPlaceholderView.
 */
export const kerisiAccountReceivableHiddenRoutes: RouteRecordRaw[] = [];
