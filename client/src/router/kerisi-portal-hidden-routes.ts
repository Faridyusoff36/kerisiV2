import type { RouteRecordRaw } from "vue-router";

/**
 * HIDDEN_PAGE_LEVEL4 Portal routes were inlined into `router/index.ts` (before
 * `...kerisiPortalHiddenRoutes`) so they sit next to LEVEL3 Portal shells and use
 * `PortalLegacyPlaceholderView` with the same props pattern.
 */
export const kerisiPortalHiddenRoutes: RouteRecordRaw[] = [];
