import type { RouteRecordRaw } from "vue-router";

/**
 * HIDDEN_PAGE_LEVEL4 Payroll routes were inlined into `router/index.ts` (before
 * `...kerisiPayrollHiddenRoutes`) so they sit next to LEVEL3 Payroll shells and use
 * `PayrollLegacyPlaceholderView` with the same props pattern.
 */
export const kerisiPayrollHiddenRoutes: RouteRecordRaw[] = [];
